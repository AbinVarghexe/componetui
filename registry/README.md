# Registry

This folder contains source-first components published by the registry. Each component is a subfolder with a `metadata.json` and the component source files.

Structure:

- `registry/<component>/metadata.json` — component metadata
- `registry/<component>/*.tsx` — source files

Example: `registry/button/`
